export type Book = {
  url: string;
  name: string;
  isbn: string;
  authors: string[],
  numberOfPages: number,
  publiser: string,
  country: string[],
  mediaType: string[],
  released: string,
  characters: string[],
  povCharacters: string[],
};